# Core dependencies for running the example agents and web apps
gradio
streamlit
pandas
numpy
pytest
# Cloud LLM providers (install whichever you use)
openai>=1.0.0         # required for OpenAI and MiniMax (OpenAI-compatible API)
anthropic             # optional: for Anthropic Claude models
